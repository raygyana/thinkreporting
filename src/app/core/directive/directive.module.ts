import { NgModule } from '@angular/core';
import { DDWDRequiredDirective } from './validations';

@NgModule({
      declarations: [
            DDWDRequiredDirective
      ],
      providers: [

      ],
      imports: [

      ],
      exports: [
            DDWDRequiredDirective
      ]
})

export class DirectiveModule { }
