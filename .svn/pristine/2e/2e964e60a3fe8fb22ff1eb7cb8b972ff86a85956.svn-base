import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BacsBillingComponent } from './bacs-billing.component';

describe('BacsBillingComponent', () => {
  let component: BacsBillingComponent;
  let fixture: ComponentFixture<BacsBillingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BacsBillingComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BacsBillingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
