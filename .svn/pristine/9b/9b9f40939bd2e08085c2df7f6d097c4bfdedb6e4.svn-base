import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AduitParagraphReportSubmitJobComponent } from './aduit-paragraph-report-submit-job.component';

describe('AduitParagraphReportSubmitJobComponent', () => {
  let component: AduitParagraphReportSubmitJobComponent;
  let fixture: ComponentFixture<AduitParagraphReportSubmitJobComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AduitParagraphReportSubmitJobComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AduitParagraphReportSubmitJobComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
