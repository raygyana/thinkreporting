import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BacklabelSubmitJobComponent } from './backlabel-submit-job.component';

describe('BacklabelSubmitJobComponent', () => {
  let component: BacklabelSubmitJobComponent;
  let fixture: ComponentFixture<BacklabelSubmitJobComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BacklabelSubmitJobComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BacklabelSubmitJobComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
