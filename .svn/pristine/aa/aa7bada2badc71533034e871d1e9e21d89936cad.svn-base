import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListExtractSubmitJobComponent } from './list-extract-submit-job.component';

describe('ListExtractSubmitJobComponent', () => {
  let component: ListExtractSubmitJobComponent;
  let fixture: ComponentFixture<ListExtractSubmitJobComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListExtractSubmitJobComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListExtractSubmitJobComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
