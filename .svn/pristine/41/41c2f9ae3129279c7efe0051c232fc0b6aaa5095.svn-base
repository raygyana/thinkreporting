import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RenewalTabComponent } from './renewal-tab.component';

describe('RenewalTabComponent', () => {
  let component: RenewalTabComponent;
  let fixture: ComponentFixture<RenewalTabComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RenewalTabComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RenewalTabComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
